# Grid Duel

A mobile-first strategy game: secretly commit power to territories, read the rival's choices, and control the grid.

## Run locally

Open `dist/index.html` in a browser, or serve the `dist` folder with any static HTTP server.

## Deploy

Vercel deploys the static app from the `dist` directory.
